#include "main.h"

/**
* _abs - computes the absolute value of a num.
* @int: parameter.
* Return: Always 0.
*/

int _abs(int)
{
	int n, abs;

	if (n < 0)
	{
		abs = n * (-1);
	}
	else
	{
		abs = n * 1;
	}

	return (0);
}
